[package]
name = "aln-codegen"
version = "0.1.0"
edition = "2021"

[dependencies]
aln-syntax = { path = "../aln-syntax" }
aln-check = { path = "../aln-check" }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }

[lib]
name = "aln_codegen"
path = "src/lib.rs"
