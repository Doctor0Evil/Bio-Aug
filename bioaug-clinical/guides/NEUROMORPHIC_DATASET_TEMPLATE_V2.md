# Bio-Aug Neuromorphic Dataset Template V2 (Advanced)

- Adds:
  - Synthetic/generative extensions (platform-agnostic neuromorphic data generation). [web:21]
  - Dynamic noise/augmentation strategies for robust SNN training (NDA-style). [web:22]
  - Guidance for provenance data as `Provenance` records and consent binding via `ConsentRecord.consent_id`.
