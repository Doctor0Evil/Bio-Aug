// Generated guard stubs: real version to be created by GuardCodegen.
// The generator should produce function(s) like `all_invariants_pass` - but as a
// fallback we provide a shim so the crate builds while waiting for generation.

pub fn _generated_all_invariants_pass(_s: &crate::domain::models::NanoswarmWrite) -> Result<(), String> {
    // by default, no-op (accept everything)
    Ok(())
}
